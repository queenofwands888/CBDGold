import React from 'react';


// Example: Move balances, tier, and wallet info here as props or context later
const Dashboard: React.FC = () => {
  // TODO: Accept balances and wallet info as props or context
  return (
    <div>
      <h2 className="text-3xl font-bold text-white mb-2">Dashboard</h2>
      {/* Add wallet, balances, and tier status UI here, as in App.tsx.old */}
    </div>
  );
};

export default Dashboard;
