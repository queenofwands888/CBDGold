/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'
import { AppFactory as _AppFactory, AppFactoryAppClientParams, AppFactoryResolveAppClientByCreatorAndNameParams, AppFactoryDeployParams, AppFactoryParams, CreateSchema } from '@algorandfoundation/algokit-utils/types/app-factory'
import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'
import SimulateResponse = modelsv2.SimulateResponse

export const APP_SPEC: Arc56Contract = {"arcs":[],"name":"CBDGoldStaking","structs":{},"methods":[{"name":"set_params","desc":"Configure staking asset and emission rate (admin only).","args":[{"name":"asset_id","type":"uint64","desc":"ASA id to stake"},{"name":"reward_rate","type":"uint64","desc":"Emission rate measured per second"}],"returns":{"type":"void"},"events":[],"actions":{"create":[],"call":[]}},{"name":"stake","desc":"Stake HEMP tokens; must be grouped with an asset transfer to the app account.","args":[],"returns":{"type":"void"},"events":[],"actions":{"create":[],"call":[]}},{"name":"unstake","desc":"Unstake previously deposited HEMP tokens back to the caller.","args":[{"name":"amount","type":"uint64","desc":"Amount of HEMP to withdraw"}],"returns":{"type":"void"},"events":[],"actions":{"create":[],"call":[]}},{"name":"claim","desc":"Claim accumulated staking rewards in HEMP.","args":[],"returns":{"type":"void"},"events":[],"actions":{"create":[],"call":[]}},{"name":"get_info","desc":"Emit staking stats (staked balance, tier, pending rewards) via logs.","args":[],"returns":{"type":"void"},"events":[],"actions":{"create":[],"call":[]}},{"name":"admin_toggle","desc":"Toggle staking availability on or off (admin only).","args":[],"returns":{"type":"void"},"events":[],"actions":{"create":[],"call":[]}}],"state":{"schema":{"global":{"ints":6,"bytes":1},"local":{"ints":5,"bytes":0}},"keys":{"global":{"total_staked":{"key":"dG90YWxfc3Rha2Vk","keyType":"AVMString","valueType":"AVMUint64","desc":"Total staked HEMP"},"staking_enabled":{"key":"c3Rha2luZ19lbmFibGVk","keyType":"AVMString","valueType":"AVMUint64","desc":"Staking toggle"},"admin_address":{"key":"YWRtaW5fYWRkcmVzcw==","keyType":"AVMString","valueType":"AVMBytes","desc":"Administrator address"},"asset_id":{"key":"YXNzZXRfaWQ=","keyType":"AVMString","valueType":"AVMUint64","desc":"ASA id being staked"},"reward_rate":{"key":"cmV3YXJkX3JhdGU=","keyType":"AVMString","valueType":"AVMUint64","desc":"Reward emission per second"},"last_reward_time":{"key":"bGFzdF9yZXdhcmRfdGltZQ==","keyType":"AVMString","valueType":"AVMUint64","desc":"Last reward accumulator update timestamp"},"acc_rpt":{"key":"YWNjX3JwdA==","keyType":"AVMString","valueType":"AVMUint64","desc":"Scaled reward-per-token accumulator"}},"local":{"staked_amount":{"key":"c3Rha2VkX2Ftb3VudA==","keyType":"AVMString","valueType":"AVMUint64","desc":"User staked HEMP"},"reward_debt":{"key":"cmV3YXJkX2RlYnQ=","keyType":"AVMString","valueType":"AVMUint64","desc":"Reward debt to avoid double counting"},"pending":{"key":"cGVuZGluZw==","keyType":"AVMString","valueType":"AVMUint64","desc":"Pending rewards awaiting claim"},"stake_timestamp":{"key":"c3Rha2VfdGltZXN0YW1w","keyType":"AVMString","valueType":"AVMUint64","desc":"Last stake timestamp"},"tier":{"key":"dGllcg==","keyType":"AVMString","valueType":"AVMUint64","desc":"User tier derived from balance"}},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"source":{"approval":"","clear":""},"bareActions":{"create":[],"call":["CloseOut","DeleteApplication","NoOp","OptIn","UpdateApplication"]}} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never

type BareCallOnComplete =
  | OnApplicationComplete.NoOpOC
  | OnApplicationComplete.OptInOC
  | OnApplicationComplete.CloseOutOC
  | OnApplicationComplete.ClearStateOC
  | OnApplicationComplete.DeleteApplicationOC

type BareCallParams = Expand<Omit<AppClientBareCallParams, 'onComplete'> & { onComplete?: BareCallOnComplete }>
type BareCallParamsWithCompilation = Expand<Omit<AppClientBareCallParams & AppClientCompilationParams, 'onComplete'> & { onComplete?: BareCallOnComplete }>
type BareCallParamsWithSend = Expand<Omit<AppClientBareCallParams & SendParams, 'onComplete'> & { onComplete?: BareCallOnComplete }>
type BareCallParamsWithSendAndCompilation = Expand<Omit<AppClientBareCallParams & AppClientCompilationParams & SendParams, 'onComplete'> & { onComplete?: BareCallOnComplete }>


/**
 * The argument types for the CbdGoldStaking contract
 */
export type CbdGoldStakingArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'set_params(uint64,uint64)void': {
      /**
       * ASA id to stake
       */
      assetId: bigint | number
      /**
       * Emission rate measured per second
       */
      rewardRate: bigint | number
    }
    'stake()void': Record<string, never>
    'unstake(uint64)void': {
      /**
       * Amount of HEMP to withdraw
       */
      amount: bigint | number
    }
    'claim()void': Record<string, never>
    'get_info()void': Record<string, never>
    'admin_toggle()void': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'set_params(uint64,uint64)void': [assetId: bigint | number, rewardRate: bigint | number]
    'stake()void': []
    'unstake(uint64)void': [amount: bigint | number]
    'claim()void': []
    'get_info()void': []
    'admin_toggle()void': []
  }
}

/**
 * The return type for each method
 */
export type CbdGoldStakingReturns = {
  'set_params(uint64,uint64)void': void
  'stake()void': void
  'unstake(uint64)void': void
  'claim()void': void
  'get_info()void': void
  'admin_toggle()void': void
}

/**
 * Defines the types of available calls and state of the CbdGoldStaking smart contract.
 */
export type CbdGoldStakingTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'set_params(uint64,uint64)void' | 'set_params', {
      argsObj: CbdGoldStakingArgs['obj']['set_params(uint64,uint64)void']
      argsTuple: CbdGoldStakingArgs['tuple']['set_params(uint64,uint64)void']
      returns: CbdGoldStakingReturns['set_params(uint64,uint64)void']
    }>
    & Record<'stake()void' | 'stake', {
      argsObj: CbdGoldStakingArgs['obj']['stake()void']
      argsTuple: CbdGoldStakingArgs['tuple']['stake()void']
      returns: CbdGoldStakingReturns['stake()void']
    }>
    & Record<'unstake(uint64)void' | 'unstake', {
      argsObj: CbdGoldStakingArgs['obj']['unstake(uint64)void']
      argsTuple: CbdGoldStakingArgs['tuple']['unstake(uint64)void']
      returns: CbdGoldStakingReturns['unstake(uint64)void']
    }>
    & Record<'claim()void' | 'claim', {
      argsObj: CbdGoldStakingArgs['obj']['claim()void']
      argsTuple: CbdGoldStakingArgs['tuple']['claim()void']
      returns: CbdGoldStakingReturns['claim()void']
    }>
    & Record<'get_info()void' | 'get_info', {
      argsObj: CbdGoldStakingArgs['obj']['get_info()void']
      argsTuple: CbdGoldStakingArgs['tuple']['get_info()void']
      returns: CbdGoldStakingReturns['get_info()void']
    }>
    & Record<'admin_toggle()void' | 'admin_toggle', {
      argsObj: CbdGoldStakingArgs['obj']['admin_toggle()void']
      argsTuple: CbdGoldStakingArgs['tuple']['admin_toggle()void']
      returns: CbdGoldStakingReturns['admin_toggle()void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * Total staked HEMP
         */
        totalStaked: bigint
        /**
         * Staking toggle
         */
        stakingEnabled: bigint
        /**
         * Administrator address
         */
        adminAddress: BinaryState
        /**
         * ASA id being staked
         */
        assetId: bigint
        /**
         * Reward emission per second
         */
        rewardRate: bigint
        /**
         * Last reward accumulator update timestamp
         */
        lastRewardTime: bigint
        /**
         * Scaled reward-per-token accumulator
         */
        accRpt: bigint
      }
      maps: {}
    }
    local: {
      keys: {
        /**
         * User staked HEMP
         */
        stakedAmount: bigint
        /**
         * Reward debt to avoid double counting
         */
        rewardDebt: bigint
        /**
         * Pending rewards awaiting claim
         */
        pending: bigint
        /**
         * Last stake timestamp
         */
        stakeTimestamp: bigint
        /**
         * User tier derived from balance
         */
        tier: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type CbdGoldStakingSignatures = keyof CbdGoldStakingTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type CbdGoldStakingNonVoidMethodSignatures = keyof CbdGoldStakingTypes['methods'] extends infer T ? T extends keyof CbdGoldStakingTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the CbdGoldStaking smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends CbdGoldStakingSignatures> = CbdGoldStakingTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the CbdGoldStaking smart contract to the method's return type
 */
export type MethodReturn<TSignature extends CbdGoldStakingSignatures> = CbdGoldStakingTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = CbdGoldStakingTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed local state of the application.
 */
export type LocalKeysState = CbdGoldStakingTypes['state']['local']['keys']


/**
 * Defines supported update method params for this smart contract
 */
export type CbdGoldStakingUpdateCallParams =
  | BareCallParams & {method?: never}
/**
 * Defines supported delete method params for this smart contract
 */
export type CbdGoldStakingDeleteCallParams =
  | BareCallParams & {method?: never}
/**
 * Defines arguments required for the deploy method.
 */
export type CbdGoldStakingDeployParams = Expand<Omit<AppFactoryDeployParams, 'createParams' | 'updateParams' | 'deleteParams'> & {
  /**
   * Update transaction parameters to use if a create needs to be issued as part of deployment; use `method` to define ABI call (if available) or leave out for a bare call (if available)
   */
  updateParams?: CbdGoldStakingUpdateCallParams
  /**
   * Delete transaction parameters to use if a create needs to be issued as part of deployment; use `method` to define ABI call (if available) or leave out for a bare call (if available)
   */
  deleteParams?: CbdGoldStakingDeleteCallParams
}>


/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the CbdGoldStaking smart contract
 */
export abstract class CbdGoldStakingParamsFactory {
}

/**
 * A factory to create and deploy one or more instance of the CBDGoldStaking smart contract and to create one or more app clients to interact with those (or other) app instances
 */
export class CbdGoldStakingFactory {
  /**
   * The underlying `AppFactory` for when you want to have more flexibility
   */
  public readonly appFactory: _AppFactory

  /**
   * Creates a new instance of `CbdGoldStakingFactory`
   *
   * @param params The parameters to initialise the app factory with
   */
  constructor(params: Omit<AppFactoryParams, 'appSpec'>) {
    this.appFactory = new _AppFactory({
      ...params,
      appSpec: APP_SPEC,
    })
  }
  
  /** The name of the app (from the ARC-32 / ARC-56 app spec or override). */
  public get appName() {
    return this.appFactory.appName
  }
  
  /** The ARC-56 app spec being used */
  get appSpec() {
    return APP_SPEC
  }
  
  /** A reference to the underlying `AlgorandClient` this app factory is using. */
  public get algorand(): AlgorandClient {
    return this.appFactory.algorand
  }
  
  /**
   * Returns a new `AppClient` client for an app instance of the given ID.
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public getAppClientById(params: AppFactoryAppClientParams) {
    return new CbdGoldStakingClient(this.appFactory.getAppClientById(params))
  }
  
  /**
   * Returns a new `AppClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public async getAppClientByCreatorAndName(
    params: AppFactoryResolveAppClientByCreatorAndNameParams,
  ) {
    return new CbdGoldStakingClient(await this.appFactory.getAppClientByCreatorAndName(params))
  }

  /**
   * Idempotently deploys the CBDGoldStaking smart contract.
   *
   * @param params The arguments for the contract calls and any additional parameters for the call
   * @returns The deployment result
   */
  public async deploy(params: CbdGoldStakingDeployParams = {}) {
    const result = await this.appFactory.deploy({
      ...params,
    })
    return { result: result.result, appClient: new CbdGoldStakingClient(result.appClient) }
  }

  /**
   * Get parameters to create transactions (create and deploy related calls) for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available deployUpdate methods
     */
    deployUpdate: {
      /**
       * Updates an existing instance of the CBDGoldStaking smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The params for a deployUpdate call
       */
      bare: (params?: BareCallParamsWithCompilation) => {
        return this.appFactory.params.bare.deployUpdate(params)
      },
    },

    /**
     * Gets available deployDelete methods
     */
    deployDelete: {
      /**
       * Deletes an existing instance of the CBDGoldStaking smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The params for a deployDelete call
       */
      bare: (params?: BareCallParams) => {
        return this.appFactory.params.bare.deployDelete(params)
      },
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
  }

  /**
   * Send calls to the current app
   */
  readonly send = {
  }

}
/**
 * A client to make calls to the CBDGoldStaking smart contract
 */
export class CbdGoldStakingClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `CbdGoldStakingClient`
   *
   * @param appClient An `AppClient` instance which has been created with the CbdGoldStaking app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `CbdGoldStakingClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }
  
  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends CbdGoldStakingNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }
  
  /**
   * Returns a new `CbdGoldStakingClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<CbdGoldStakingClient> {
    return new CbdGoldStakingClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `CbdGoldStakingClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<CbdGoldStakingClient> {
    return new CbdGoldStakingClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available update methods
     */
    update: {
      /**
       * Updates an existing instance of the CBDGoldStaking smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The update result
       */
      bare: (params?: BareCallParamsWithCompilation) => {
        return this.appClient.params.bare.update(params)
      },
    },

    /**
     * Gets available delete methods
     */
    delete: {
      /**
       * Deletes an existing instance of the CBDGoldStaking smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The delete result
       */
      bare: (params?: BareCallParams) => {
        return this.appClient.params.bare.delete(params)
      },
    },

    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the CBDGoldStaking smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The optIn result
       */
      bare: (params?: BareCallParams) => {
        return this.appClient.params.bare.optIn(params)
      },
    },

    /**
     * Gets available closeOut methods
     */
    closeOut: {
      /**
       * Makes a close out call to an existing instance of the CBDGoldStaking smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The closeOut result
       */
      bare: (params?: BareCallParams) => {
        return this.appClient.params.bare.closeOut(params)
      },
    },

    /**
     * Makes a clear_state call to an existing instance of the CBDGoldStaking smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: BareCallParams) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the CBDGoldStaking smart contract using a bare call.
     *
     * @param params The params for the bare (raw) call
     * @returns The call result
     */
    bare: (params?: BareCallParams) => {
      return this.appClient.params.bare.call(params)
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available update methods
     */
    update: {
      /**
       * Updates an existing instance of the CBDGoldStaking smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The update result
       */
      bare: (params?: BareCallParamsWithCompilation) => {
        return this.appClient.createTransaction.bare.update(params)
      },
    },

    /**
     * Gets available delete methods
     */
    delete: {
      /**
       * Deletes an existing instance of the CBDGoldStaking smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The delete result
       */
      bare: (params?: BareCallParams) => {
        return this.appClient.createTransaction.bare.delete(params)
      },
    },

    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the CBDGoldStaking smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The optIn result
       */
      bare: (params?: BareCallParams) => {
        return this.appClient.createTransaction.bare.optIn(params)
      },
    },

    /**
     * Gets available closeOut methods
     */
    closeOut: {
      /**
       * Makes a close out call to an existing instance of the CBDGoldStaking smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The closeOut result
       */
      bare: (params?: BareCallParams) => {
        return this.appClient.createTransaction.bare.closeOut(params)
      },
    },

    /**
     * Makes a clear_state call to an existing instance of the CBDGoldStaking smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: BareCallParams) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the CBDGoldStaking smart contract using a bare call.
     *
     * @param params The params for the bare (raw) call
     * @returns The call result
     */
    bare: (params?: BareCallParams) => {
      return this.appClient.createTransaction.bare.call(params)
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available update methods
     */
    update: {
      /**
       * Updates an existing instance of the CBDGoldStaking smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The update result
       */
      bare: (params?: BareCallParamsWithSendAndCompilation) => {
        return this.appClient.send.bare.update(params)
      },
    },

    /**
     * Gets available delete methods
     */
    delete: {
      /**
       * Deletes an existing instance of the CBDGoldStaking smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The delete result
       */
      bare: (params?: BareCallParamsWithSend) => {
        return this.appClient.send.bare.delete(params)
      },
    },

    /**
     * Gets available optIn methods
     */
    optIn: {
      /**
       * Opts the user into an existing instance of the CBDGoldStaking smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The optIn result
       */
      bare: (params?: BareCallParamsWithSend) => {
        return this.appClient.send.bare.optIn(params)
      },
    },

    /**
     * Gets available closeOut methods
     */
    closeOut: {
      /**
       * Makes a close out call to an existing instance of the CBDGoldStaking smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The closeOut result
       */
      bare: (params?: BareCallParamsWithSend) => {
        return this.appClient.send.bare.closeOut(params)
      },
    },

    /**
     * Makes a clear_state call to an existing instance of the CBDGoldStaking smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: BareCallParamsWithSend) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the CBDGoldStaking smart contract using a bare call.
     *
     * @param params The params for the bare (raw) call
     * @returns The call result
     */
    bare: (params?: BareCallParamsWithSend) => {
      return this.appClient.send.bare.call(params)
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new CbdGoldStakingClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current CBDGoldStaking app
   */
  state = {
    /**
     * Methods to access global state for the current CBDGoldStaking app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalStaked: result.total_staked,
          stakingEnabled: result.staking_enabled,
          adminAddress: new BinaryStateValue(result.admin_address),
          assetId: result.asset_id,
          rewardRate: result.reward_rate,
          lastRewardTime: result.last_reward_time,
          accRpt: result.acc_rpt,
        }
      },
      /**
       * Get the current value of the total_staked key in global state
       */
      totalStaked: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("total_staked")) as bigint | undefined },
      /**
       * Get the current value of the staking_enabled key in global state
       */
      stakingEnabled: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("staking_enabled")) as bigint | undefined },
      /**
       * Get the current value of the admin_address key in global state
       */
      adminAddress: async (): Promise<BinaryState> => { return new BinaryStateValue((await this.appClient.state.global.getValue("admin_address")) as Uint8Array | undefined) },
      /**
       * Get the current value of the asset_id key in global state
       */
      assetId: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("asset_id")) as bigint | undefined },
      /**
       * Get the current value of the reward_rate key in global state
       */
      rewardRate: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("reward_rate")) as bigint | undefined },
      /**
       * Get the current value of the last_reward_time key in global state
       */
      lastRewardTime: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("last_reward_time")) as bigint | undefined },
      /**
       * Get the current value of the acc_rpt key in global state
       */
      accRpt: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("acc_rpt")) as bigint | undefined },
    },
    /**
     * Methods to access local state for the current CBDGoldStaking app
     */
    local: (address: string | Address) => {
      const encodedAddress = typeof address === 'string' ? address : encodeAddress(address.publicKey)
      return {
        /**
         * Get all current keyed values from local state
         */
        getAll: async (): Promise<Partial<Expand<LocalKeysState>>> => {
          const result = await this.appClient.state.local(encodedAddress).getAll()
          return {
            stakedAmount: result.staked_amount,
            rewardDebt: result.reward_debt,
            pending: result.pending,
            stakeTimestamp: result.stake_timestamp,
            tier: result.tier,
          }
        },
        /**
         * Get the current value of the staked_amount key in local state
         */
        stakedAmount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("staked_amount")) as bigint | undefined },
        /**
         * Get the current value of the reward_debt key in local state
         */
        rewardDebt: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("reward_debt")) as bigint | undefined },
        /**
         * Get the current value of the pending key in local state
         */
        pending: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("pending")) as bigint | undefined },
        /**
         * Get the current value of the stake_timestamp key in local state
         */
        stakeTimestamp: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("stake_timestamp")) as bigint | undefined },
        /**
         * Get the current value of the tier key in local state
         */
        tier: async (): Promise<bigint | undefined> => { return (await this.appClient.state.local(encodedAddress).getValue("tier")) as bigint | undefined },
      }
    },
  }

  public newGroup(): CbdGoldStakingComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a bare method call to the CBDGoldStaking contract
       */
      bare(params: BareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.bare(params)))
        return this
      },
      get update() {
        return {
          bare: (params?: AppClientBareCallParams & AppClientCompilationParams ) => {
            promiseChain = promiseChain.then(async () => composer.addAppUpdate(await client.params.update.bare(params)))
            return this
          },
        }
      },
      get delete() {
        return {
          bare: (params?: AppClientBareCallParams ) => {
            promiseChain = promiseChain.then(() => composer.addAppDelete(client.params.delete.bare(params)))
            return this
          },
        }
      },
      get optIn() {
        return {
          bare: (params?: AppClientBareCallParams ) => {
            promiseChain = promiseChain.then(() => composer.addAppCall(client.params.optIn.bare(params)))
            return this
          },
        }
      },
      get closeOut() {
        return {
          bare: (params?: AppClientBareCallParams ) => {
            promiseChain = promiseChain.then(() => composer.addAppCall(client.params.closeOut.bare(params)))
            return this
          },
        }
      },
      /**
       * Add a clear state call to the CBDGoldStaking contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as CbdGoldStakingComposer
  }
}
export type CbdGoldStakingComposer<TReturns extends [...any[]] = []> = {
  /**
   * Gets available delete methods
   */
  readonly delete: {
    /**
     * Deletes an existing instance of the CBDGoldStaking smart contract using a bare call.
     *
     * @param args The arguments for the bare call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    bare(params?: AppClientBareCallParams ): CbdGoldStakingComposer<[...TReturns, undefined]>
  }

  /**
   * Gets available optIn methods
   */
  readonly optIn: {
    /**
     * Opts the user into an existing instance of the CBDGoldStaking smart contract using a bare call.
     *
     * @param args The arguments for the bare call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    bare(params?: AppClientBareCallParams ): CbdGoldStakingComposer<[...TReturns, undefined]>
  }

  /**
   * Gets available closeOut methods
   */
  readonly closeOut: {
    /**
     * Makes a close out call to an existing instance of the CBDGoldStaking smart contract using a bare call.
     *
     * @param args The arguments for the bare call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    bare(params?: AppClientBareCallParams ): CbdGoldStakingComposer<[...TReturns, undefined]>
  }

  /**
   * Gets available update methods
   */
  readonly update: {
    /**
     * Updates an existing instance of the CBDGoldStaking smart contract using a bare call.
     *
     * @param args The arguments for the bare call
     * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
     */
    bare(params?: AppClientBareCallParams ): CbdGoldStakingComposer<[...TReturns, undefined]>
  }

  /**
   * Makes a clear_state call to an existing instance of the CBDGoldStaking smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): CbdGoldStakingComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): CbdGoldStakingComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<CbdGoldStakingComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<CbdGoldStakingComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<CbdGoldStakingComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<CbdGoldStakingComposerResults<TReturns>>
}
export type CbdGoldStakingComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

