import { describe, it } from 'vitest';

// Duplicate legacy file kept to avoid import errors in glob; real tests live in persistence.test.tsx
describe.skip('legacy persistence test placeholder', () => {
  it('skipped', () => { });
});
